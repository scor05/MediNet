services:
  backend:
    build: ./backend
    container_name: medinet_backend
    ports:
      - "8067:8000"
    volumes:
      - ./backend:/app
      - /app/vendor
    env_file:
      - ./backend/.env